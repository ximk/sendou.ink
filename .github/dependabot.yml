version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 8
    groups:
      minor-and-patch:
        update-types:
          - "minor"
          - "patch"
        exclude-patterns:
          - "@biomejs/*"
    ignore:
      - dependency-name: "tldraw"
      - dependency-name: "@tldraw/*"
